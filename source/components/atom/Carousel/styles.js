import {StyleSheet} from 'react-native';
import AppColors from '../../../AppConstants/appColors';

export default StyleSheet.create({
  cerousContainer: {
    flex: 1,
    // alignItems:'center',
    // backgroundColor:'red',
    // marginVertical:10
    //justifyContent:'flex-end'
  },

  container: {
    width: '100%',
    height: '100%',
  },

  activeDotStyle: {
    width: 10, // Change these values to increase the active dot's size
    height: 10,
  },
});
